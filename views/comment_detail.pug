extends layout

block content

    h5 Comment detail: 
    p #{comment.text}
    br
    div(style='display:flex;gap:.5rem')
        p #{comment.owner}
            p commented at: #{comment.timestamp}
    if c_user
        p
            a(href=comment.url+'/delete') Delete Comment